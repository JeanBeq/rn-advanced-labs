import { StyleSheet } from 'react-native';
import ProfileCard from '../tp1-profile-card';

export default function HomeScreen() {
  return (
    <ProfileCard />
  );
}
  
const styles = StyleSheet.create({
});
